services:
  - type: web
    name: podcast-transcriber
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 3600 --workers 2 --bind 0.0.0.0:$PORT
    plan: free
